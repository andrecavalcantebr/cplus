// C preprocessor line:
#define SOME_MACRO 42

// C declaration-like line:
extern int some_c_symbol;

// Function pointer typedef in C (should go to c_chunk):
typedef int (*cmp_fn)(const void*, const void*);

// Struct tag line (C-ish):
struct FooTag;
